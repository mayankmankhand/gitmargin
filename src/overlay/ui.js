// Placeholder: the Hairline UI lands in step 4 of plans/PLAN-issue-3.md.
// Step 3 ships capture and export only, so this mounts nothing.
export function mountUi() {
  return { placeholder: true };
}
